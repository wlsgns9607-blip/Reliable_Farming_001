rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [워크어라운드] 파이어베이스 설정에서 익명 로그인이 비활성화되어 있는 경우를 위해
    // 임시로 공개 읽기/쓰기를 허용합니다. (실제 배포 시에는 보안 규칙을 강화해야 합니다.)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
